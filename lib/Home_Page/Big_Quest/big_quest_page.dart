import 'package:flutter/material.dart';

class BigQuestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Big Quest")),
      body: Center(child: Text("Ini adalah halaman Big Quest")),
    );
  }
}
